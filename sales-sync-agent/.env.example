# === Google Sheets ===
# Spreadsheet ID (from the sheet URL: https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit)
GOOGLE_SPREADSHEET_ID=your-spreadsheet-id-here

# Path to your Google service-account JSON key file
GOOGLE_SERVICE_ACCOUNT_KEY=credentials.json

# Sheet name (tab) — defaults to "Sheet1"
SHEET_NAME=Sheet1

# Cell references for each channel's sales count.
# Adjust these to match your actual spreadsheet layout.
SHOPIFY_CELL=Sheet1!B2
ETSY_CELL=Sheet1!B3

# === Shopify ===
# Webhook secret from Shopify admin (Settings > Notifications > Webhooks)
SHOPIFY_WEBHOOK_SECRET=your-shopify-webhook-secret

# === Etsy / Gmail ===
# Path to your Gmail OAuth2 client credentials JSON
GMAIL_OAUTH_CREDENTIALS=gmail_credentials.json

# Etsy notification sender email
ETSY_SENDER_EMAIL=transaction@etsy.com

# How often (in seconds) to poll Gmail for new Etsy order emails
ETSY_POLL_INTERVAL=60

# Gmail label used to mark processed order emails
ETSY_PROCESSED_LABEL=SalesSyncProcessed

# === Server ===
PORT=5000
HOST=0.0.0.0
